---
type: Log
title: Wiki Changelog
description: Chronological record of what changed in this wiki and when, newest first.
generated: { by: claude-code/opus-5, at: 2026-09-01T19:28:00Z }
status: stable
---

## 2026-09-08
* **Ingest**: Added Anthropic's ["The AI-Native SDLC Playbook"](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/anthropic-ai-native-sdlc-playbook.md) (Claude Academy, retrieved 2026-09-08) to `references/` — all fourteen lesson pages captured from the course's server-rendered HTML and concatenated in course order under the six stage headings. Created the `AI-Native SDLC/` folder with a page for the model itself, [The AI-Native SDLC](</AI-Native SDLC/ai-native-sdlc.md>), plus one page per play: [capture intent](</AI-Native SDLC/capture-intent.md>), [requirements and design](</AI-Native SDLC/requirements-and-design.md>), [plan mode](</AI-Native SDLC/plan-mode.md>), [the CLAUDE.md](</AI-Native SDLC/claude-md.md>), [skills as institutional knowledge](</AI-Native SDLC/skills-as-institutional-knowledge.md>), [parallel sessions and subagents](</AI-Native SDLC/parallel-sessions-and-subagents.md>), [the feedback loop](</AI-Native SDLC/feedback-loop.md>), [continuous evals](</AI-Native SDLC/continuous-evals.md>), [PR review](</AI-Native SDLC/ai-pr-review.md>), [hooks as approval gates](</AI-Native SDLC/hooks-as-approval-gates.md>), [CI/CD integration](</AI-Native SDLC/ci-cd-integration.md>), and [closing the loop](</AI-Native SDLC/closing-the-loop.md>). Catalogued under a new "AI-native SDLC" section in the root `index.md`, with the folder carrying its own `index.md` grouped by stage. The twelve plays use the existing `Practice` type; only the top page needed `Overview`.
* **Note**: The organizing claim worth keeping is the **committed artifact chain** — `intent.md` → `spec.md` → `plan.md` → the diff and its tests → the PR and its findings → the incident record → the next `intent.md`. Each stage ends by writing a file to version control and the next begins by reading it, which is what lets the playbook argue the old control objectives survive under new enforcement: the chain of commits *is* the audit trail. It is [context engineering](/context-engineering.md) applied to a process rather than a session.
* **Note**: The distinction the playbook is most careful about, and the one that reframes [Agent Skills](/agent-skills.md) as governance rather than productivity tooling: **a skill is an advisory control and a hook is a deterministic one**. A skill makes Claude likely to apply a policy while the code is written; nothing forces a session to comply. A policy that must always hold needs a hook behind it — the skill makes violations rare, the hook makes them close to impossible. Recorded on both the [skills play](</AI-Native SDLC/skills-as-institutional-knowledge.md>) and the [hooks play](</AI-Native SDLC/hooks-as-approval-gates.md>).
* **Note**: The [hooks play](</AI-Native SDLC/hooks-as-approval-gates.md>) carries the course's fullest artifact, a managed-settings file for a regulated enterprise, annotated key by key. Two points there are easy to miss and worth flagging: `sandbox` and `permissions.deny` enforce **one** objective at **two** layers (a tool-level deny on `WebFetch` doesn't stop a shell command reaching the network, and `credentials` covers what the file-tool deny rules miss for `~/.ssh` and `~/.aws/credentials`); and `allowManagedHooksOnly` silently invalidates the repo-level `.claude/settings.json` gate the same lesson shows first, so the gate has to be redefined in the managed file's own `hooks` block.
* **Note**: Three pages record the playbook's own framing against the [agentic design patterns](</Agentic Design Patterns/choosing-a-pattern.md>) vocabulary already in the wiki. Parallel Claude Code sessions are *not* a multi-agent system — no orchestrator, and the human is the only shared state — while subagents are the [parallel pattern](</Agentic Design Patterns/parallel.md>) with context isolation as the point. The [feedback loop](</AI-Native SDLC/feedback-loop.md>) is [iterative refinement](</Agentic Design Patterns/iterative-refinement.md>) with a deterministic evaluator rather than a model as judge. The sigma tier ladder in [closing the loop](</AI-Native SDLC/closing-the-loop.md>) is [human-in-the-loop](</Agentic Design Patterns/human-in-the-loop.md>) expressed as a threshold, with autonomy granted in proportion to how far the metric has moved.
* **Note**: Two caveats recorded on the [overview](</AI-Native SDLC/ai-native-sdlc.md>). This is a vendor's playbook for its own tools, and its value is the specificity rather than the thesis. And its measurement guidance is mostly proxy metrics readable from systems that already exist (Git timestamps, PR metadata, CI check runs, the OpenTelemetry export) — the leading indicators largely measure cycle time, and only the lagging ones (rework rate, change failure rate, escaped defects, DORA) speak to outcomes.
* **Note**: The [plan mode play](</AI-Native SDLC/plan-mode.md>) holds the course's only sustained treatment of legacy systems, which is the part most likely to matter in an enterprise adoption: for each artifact, name **one** system the source of truth and let the others hold a copy or a link, with the choice differing per artifact. The three configurations are the repo as truth, the legacy system as truth (Claude reading and writing back through an [MCP](/model-context-protocol.md) connector in the same session), and linkage — record ID in the artifact, commit SHA in the record — as the transitional minimum.
* **Note**: No diagrams yet. The course carries three figures (the moved bottleneck, line versus loop, and the play dependency graph); the reference file points at them by URL rather than embedding copies.

## 2026-09-07
* **Update**: Replaced the twelve per-pattern glyph crops with faithful recreations of Google Cloud's own figures, one Excalidraw source per pattern — [single agent](/Diagrams/single-agent.excalidraw), [sequential](/Diagrams/sequential.excalidraw), [parallel](/Diagrams/parallel.excalidraw), [loop](/Diagrams/loop.excalidraw), [review and critique](/Diagrams/review-and-critique.excalidraw), [iterative refinement](/Diagrams/iterative-refinement.excalidraw), [coordinator](/Diagrams/coordinator.excalidraw), [hierarchical task decomposition](/Diagrams/hierarchical-task-decomposition.excalidraw), [swarm](/Diagrams/swarm.excalidraw), [ReAct](/Diagrams/react.excalidraw), [human-in-the-loop](/Diagrams/human-in-the-loop.excalidraw), and [custom logic](/Diagrams/custom-logic.excalidraw). Each pattern page now embeds its own `_attachments/<pattern>.png` and its callout points at its own source instead of a crop of the shared map; the twelve `.jpg` crops are removed. Like the MCP hub diagram these are **replications** of source figures rather than original arguments, which `Diagrams/index.md` records — [choosing-a-pattern.excalidraw](/Diagrams/choosing-a-pattern.excalidraw) keeps its own page and no longer backs anything else.
* **Note**: Redrawn from the source images in stock Excalidraw colors (blue for the driving agent, green for the coordinator and success paths, red for rejection, pastel panels for the grouped regions), so they theme-adapt on the site the way the earlier diagrams do — the originals' flat white-and-blue would not.
* **Ingest**: Added five sources on Agent Skills — Anthropic's [Agent Skills overview](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/anthropic-agent-skills-overview.md) and [Skill authoring best practices](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/anthropic-agent-skills-best-practices.md), the [Claude Code skills documentation](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/claude-code-skills.md), and agentskills.io's [specification](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/agentskills-io-specification.md) and [best practices for skill creators](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/agentskills-io-best-practices.md) — all captured verbatim from the markdown source each docs site serves at the same path with a `.md` suffix. Created [Agent Skills](/agent-skills.md) (what a skill is, the three levels of progressive disclosure, the open `SKILL.md` spec, and Claude Code's discovery rules, extended frontmatter, invocation control, and context budgets) and [Agent Skills Best Practices](/agent-skills-best-practices.md) (sourcing, conciseness, scoping, calibrating control, discovery, structure, instruction patterns, executable code, and evaluation-driven iteration). Both catalogued under a new "Agent skills" section in the root `index.md`.
* **Note**: The user asked for the overview page from Anthropic's docs alone; the [spec](https://agentskills.io/specification) and the [Claude Code skills page](https://code.claude.com/docs/en/skills) were added because that page can't answer "the standard specification and organization for Claude Code users" on its own — the overview gives the two required frontmatter fields and the two skills directories, while the field constraints, the `scripts/`/`references/`/`assets/` conventions, and the precedence and naming rules live in the other two.
* **Note**: The two best-practice sources overlap heavily but split on one thing worth keeping: agentskills.io names the sourcing failure Anthropic's guide assumes away — a skill generated by an LLM with no domain context reproduces what the reading agent already knows, and so earns nothing. Its "gotchas section" framing (environment facts that defy reasonable assumptions, kept in `SKILL.md` because the agent won't know to go looking for a trap it doesn't know exists) has no equivalent in Anthropic's guide.
* **Note**: Recorded the portability trap on [Agent Skills](/agent-skills.md): Claude Code accepts roughly twenty frontmatter fields, but claude.ai uploads, the Skills API, and `package_skill.py` accept only the spec's six and fail with a hard `Unexpected key(s)` error rather than ignoring the rest. Skills written to the spec run in Claude Code unchanged; skills written to Claude Code's full field set don't travel. Also noted that workspace trust doesn't gate a project skill's `allowed-tools`, so a skill checked into a repository can grant itself broad tool access on a `-p` run in an untrusted folder.
* **Fix**: Pinned the meaning of "bundle" in `CLAUDE.md` — it means this repo, always, and is never nested. A category folder like `Agentic Design Patterns/` is a folder, not "the X bundle," even though it carries its own `index.md`; only the root declares `okf_version`, which is what makes it the bundle root. The word is now reserved for places where OKF structure is the subject, with "in this wiki" the wording for ordinary cross-references on concept pages. Corrected the two slips that prompted this: the 2026-09-06 ingest entry below called `Agentic Design Patterns/` a "bundle," and [Single-Agent Pattern](</Agentic Design Patterns/single-agent.md>) said "everything else in this bundle" where it meant the other patterns — ambiguous anywhere, and especially so on a page that lives inside a subdirectory. [Context Engineering](/context-engineering.md) reworded to match.
* **Ingest**: Added the MCP documentation's [intro](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/mcp-getting-started-intro.md) and [architecture overview](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/mcp-architecture-overview.md) (both version 2026-07-28) to `references/`, and created [Model Context Protocol (MCP)](/model-context-protocol.md) — the gap flagged in the A2A ingest, since three pages were already leaning on MCP without one of their own. Covers the USB-C framing from the intro plus the architecture's core concepts: participants (host/client/server, one client per server), the data and transport layers, statelessness and `server/discover`, the server primitives (tools, resources, prompts) and client primitive (elicitation), and opt-in notifications. Cross-linked from [A2A](/a2a-protocol.md) and [Context Engineering](/context-engineering.md), which now point at a real page instead of naming MCP in passing.
* **Note**: The captured docs are version **2026-07-28**, which differs from widely circulated accounts of MCP in ways the page flags explicitly: the protocol is now stateless (every request carries its version and capabilities in `_meta`, with `server/discover` replacing the `initialize` handshake), and the **sampling** and **logging** client primitives are **deprecated** — elicitation is the only client primitive left. Worth re-checking against the spec before relying on any older MCP summary.
* **Update**: Added [a replication of the official MCP hub diagram](/Diagrams/model-context-protocol.excalidraw) to the MCP page — AI applications left, data sources and tools right, bidirectional flow through one protocol in the middle. Unlike the other diagrams in this bundle it reproduces a source figure rather than making its own argument, which is recorded in `Diagrams/index.md` so the distinction stays visible. Redrawn in stock Excalidraw colors so it adapts to the site's dark mode.
* **Ingest**: Added three sources on context and prompt engineering — the [two June 2025 posts on X](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/context-engineering-coinage-tweets.md) that named context engineering, [Anthropic's "Effective context engineering for AI agents"](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/anthropic-effective-context-engineering.md) (2025-09-29), and an excerpt of [Wikipedia's "Prompt engineering"](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/wikipedia-prompt-engineering.md) (revision 1373316427). Created [Context Engineering](/context-engineering.md) and [Prompt Engineering](/prompt-engineering.md), catalogued under a new "Practices" section in the root `index.md`. Introduced `Practice` as an OKF type and recorded it in `CLAUDE.md`.
* **Correction**: The context-engineering page credits **Tobi Lütke** with proposing the term on 2025-06-19, not Karpathy. Karpathy's much-cited post six days later is a quote-tweet opening "+1 for 'context engineering' over 'prompt engineering'" — he seconded it and supplied the definition that stuck, but the coinage isn't his. Both posts are captured in one reference file because the coinage and the endorsement are a single unit of provenance. Also noted the half of Karpathy's post that usually gets dropped in quotation: context engineering is "just one small piece" of the software layer around LLM calls, alongside control flow, model dispatch, guardrails, and evals.
* **Update**: Recorded the convergence between Anthropic's long-horizon techniques and the context-engineering vocabulary already on [Multi-Agent Systems](</Agentic Design Patterns/multi-agent-systems.md>) from Google Cloud's guidance — sub-agents/*isolating*, note-taking/*persisting*, compaction/*compressing* are the same triad reached independently by two sources.
* **Note**: The [prompt engineering](/prompt-engineering.md) page deliberately does **not** carry `status: deprecated`. The sources don't support it, and OKF `status` describes the document's lifecycle rather than the subject's. The page instead separates three things that are usually collapsed: the job title (effectively obsolete, per the WSJ via Wikipedia), the term as the name of the discipline (superseded by context engineering), and the practice itself (alive, and a component of context engineering — Anthropic's own guidance still devotes sections to system prompts and few-shot examples). What actually eroded is manual trial-and-error prompt tinkering for everyday tasks; automated prompt optimization and prompt-injection defense are both growing.

## 2026-09-06
* **Ingest**: Added the A2A protocol's own ["What is A2A?"](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/a2a-protocol-what-is-a2a.md) to `references/` as a verbatim capture, and wrote [A2A (Agent2Agent) Protocol](/a2a-protocol.md) from it — the problems A2A solves, its position beside MCP in the agent stack, the five design principles, and the four-step request lifecycle. Introduced `Protocol` as an OKF type and recorded it in `CLAUDE.md`; opened a "Protocols" section in the root `index.md` to hold it. Cross-linked it to [Multi-Agent Systems](</Agentic Design Patterns/multi-agent-systems.md>), the [coordinator pattern](</Agentic Design Patterns/coordinator.md>) — the trip-planning example is that pattern with the specialists in other organizations — and [GAIL Module 5](</Google GAIL/module-5-genai-agents.md>), whose three agent components (model, tools, reasoning loop) the A2A stack extends with a layer that framing has no name for. The page argues that A2A's real subject is *organizational* boundaries rather than architectural ones; MCP has no page of its own yet, which is now the most obvious gap in this bundle.
* **Update**: Added a diagram to [A2A (Agent2Agent) Protocol](/a2a-protocol.md) carrying the page's three arguments on one canvas. It leads with the comparison the source only makes in prose — an agent squeezed through a tool interface, with what that discards named in red, beside two agents holding a multi-turn exchange. The center makes MCP and A2A perpendicular: down to stateless tools, across a dashed organizational boundary to peer agents. The right column runs the lifecycle as a timeline with the real spec surface (the agent card fields, `securitySchemes` → JWT, `sendMessage`, and the SSE event sequence), and the agent-stack summary sits under it. The card artifact shows only fields the source actually names, rather than an invented JSON payload.
* **Update**: Added a diagram to [Choosing an Agentic Design Pattern](</Agentic Design Patterns/choosing-a-pattern.md>) mapping all twelve patterns at once. It argues the taxonomy rather than listing it: the patterns sit along a single axis — whether code or a model decides what runs next — hue encodes which family a pattern belongs to, and each pattern is drawn as the shape of its own architecture (chain, fan, cycle, tree, mesh, gate, branch), so the glyphs alone carry the structure. Every pattern carries a one-line "use when", the loop and swarm carry their failure mode, and the source's one explicit recommendation — start with a single agent — is the diagram's only hero element.
* **Update**: Gave each of the twelve pattern pages its own glyph image, cropped from that same map — `_attachments/<pattern>.jpg`, embedded above the first section with a `[!diagram]` callout back to the shared source. The crops aren't pixel-cut from the PNG; each one is rendered natively from just that pattern's elements in `Diagrams/choosing-a-pattern.excalidraw`, so they stay sharp and the master file remains the single source for all thirteen images. Edit the map and the crops need re-exporting.
* **Ingest**: Added Google Cloud's ["Choose a design pattern for your agentic AI system"](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/google-cloud-agentic-design-patterns.md) (Architecture Center, last reviewed 2026-05-28) to `references/` as a verbatim capture. Created the `Agentic Design Patterns/` folder with a page for each of the twelve patterns in the doc — [single agent](</Agentic Design Patterns/single-agent.md>), [sequential](</Agentic Design Patterns/sequential.md>), [parallel](</Agentic Design Patterns/parallel.md>), [loop](</Agentic Design Patterns/loop.md>), [review and critique](</Agentic Design Patterns/review-and-critique.md>), [iterative refinement](</Agentic Design Patterns/iterative-refinement.md>), [coordinator](</Agentic Design Patterns/coordinator.md>), [hierarchical task decomposition](</Agentic Design Patterns/hierarchical-task-decomposition.md>), [swarm](</Agentic Design Patterns/swarm.md>), [ReAct](</Agentic Design Patterns/react.md>), [human-in-the-loop](</Agentic Design Patterns/human-in-the-loop.md>), and [custom logic](</Agentic Design Patterns/custom-logic.md>) — plus two pages the patterns kept leaning on: [Multi-Agent Systems](</Agentic Design Patterns/multi-agent-systems.md>) for the umbrella concept and context engineering, and [Choosing an Agentic Design Pattern](</Agentic Design Patterns/choosing-a-pattern.md>) for the doc's selection framework. Cross-linked ReAct and human-in-the-loop to the [GAIL module 5](</Google GAIL/module-5-genai-agents.md>) and [module 2](</Google GAIL/module-2-foundational-concepts.md>) treatments of the same ideas. Introduced `Pattern` as an OKF type and recorded it in `CLAUDE.md`. Noted on the [swarm](</Agentic Design Patterns/swarm.md>) page that the source's own wording conflicts on whether a swarm has a coordinator — it describes the dispatcher as non-orchestrating, then routes the final response "through the coordinator agent."
* **Update**: Split [Google Cloud GAIL Certification](/google-GAIL.md) back out into a `Google GAIL/` folder, one file per module — [Module 1](</Google GAIL/module-1-beyond-the-chatbot.md>), [Module 2](</Google GAIL/module-2-foundational-concepts.md>), [Module 3](</Google GAIL/module-3-navigate-the-landscape.md>), [Module 4](</Google GAIL/module-4-genai-apps.md>), [Module 5](</Google GAIL/module-5-genai-agents.md>) — with its own `index.md` cataloguing them. Dropped the certification-overview content (what the cert is, exam format, course objectives, study-resource links) that previously sat before Module 1, since it wasn't pulling its weight as study material. Root `index.md`'s Certifications section now lists all five modules directly instead of the one consolidated page.

## 2026-09-02
* **Fix**: Removed the leading `# Title` H1 from [The LLM Wiki Pattern](/llm-wiki-pattern.md), [Open Knowledge Format (OKF)](/open-knowledge-format.md), [Wiki Operations](/wiki-operations.md), [Operating This Wiki](/operating-this-wiki.md), and [Google Cloud GAIL Certification](/google-GAIL.md) — the site's `article-title` component already renders frontmatter `title` as the page H1 above the properties box, so every one of these was rendering its title twice. `index.md` and `log.md` were already clean. Added a rule to `CLAUDE.md` (next to the frontmatter block, and in the `log.md` section) so future pages don't reintroduce this, and fixed `CLAUDE.md`'s own `log.md` template example, which still showed the offending `# Wiki Update Log` line.
* **Update**: Added a "Type examples" subsection to [Open Knowledge Format (OKF)](/open-knowledge-format.md)'s Design principles — the blog's illustrative type list (table, dataset, metric, playbook, runbook, API) and its point that `type` isn't a closed enum (the worked example uses a producer-defined `BigQuery Table`, not generic `Table`). Filled a gap rather than creating a new page for it, after weighing a separate "types" note against extending this one.
* **Update**: Consolidated the six GAIL certification pages (overview + five module study guides) into a single page, [Google Cloud GAIL Certification](/google-GAIL.md). The six separate `google-GAIL-*.md` files are removed; all content now lives under `##`/`###`-nested module sections on one page, with the same five source citations. Root `index.md` updated to link the one consolidated page instead of six.
* **Ingest**: Added the five Google Cloud "Generative AI Leader (ILT)" course slide decks as sources (`references/genai-leader-module-1-slides.md` through `module-5-slides.md`, transcribed from the official PDFs). Created bullet-point study guides for each module — [Module 1](/google-GAIL-module-1-beyond-the-chatbot.md), [Module 2](/google-GAIL-module-2-foundational-concepts.md), [Module 3](/google-GAIL-module-3-navigate-the-landscape.md), [Module 4](/google-GAIL-module-4-genai-apps.md), [Module 5](/google-GAIL-module-5-genai-agents.md) — plus an overview page, [Google Cloud Generative AI Leader Certification](/google-GAIL-cert.md), with exam format and study-resource links. All marked `status: draft`; quiz answers in the source decks weren't visually distinguishable after text extraction, so several were inferred from context rather than confirmed. Catalogued in the new "Certifications" section of the root `index.md`.
* **Update**: Corrected `Diagrams/` to hold only `.excalidraw` sources — no images. Moved `llm-wiki-pattern.png` out to a new `_attachments/` folder, the wiki's general home for every image going forward. Added [Diagrams/index.md](/Diagrams/index.md) cataloguing each source and the note it belongs to. `CLAUDE.md` and `README.md` updated accordingly.
* **Update**: Standardized how concept pages link to their diagram's interactive version — a `[!diagram]` callout under the PNG, not a plain prose sentence or a link wrapped around the image. `ismailkhan.xyz` now defines a custom `diagram` callout type (`quartz/styles/callouts.scss`) with its own icon and color. [The LLM Wiki Pattern](/llm-wiki-pattern.md) uses the new pattern; `CLAUDE.md` documents it as the rule for every future diagram.
* **Update**: Moved the diagram source and its PNG from the repo root and `attachments/` into a new `Diagrams/` folder — the one standing exception to the flat directory layout. The Excalidraw plugin has no frontmatter hook to hide individual diagram pages from the site's Explorer sidebar, so grouping every diagram under one directory keeps the sidebar to a single collapsible "Diagrams" entry instead of one flat item per drawing. [The LLM Wiki Pattern](/llm-wiki-pattern.md) now embeds its PNG wrapped in a link to the `.excalidraw` file, so clicking the image opens the interactive, pan-and-zoom version directly.
* **Update**: Added a diagram to [The LLM Wiki Pattern](/llm-wiki-pattern.md), arguing the note's central inversion visually — retrieval-only does its work after every question, the wiki compiles once at ingest. The Excalidraw source lives beside the note and publishes as an interactive, pan-and-zoom page; the PNG in `attachments/` is the inline figure. Diagrams are authored with the `excalidraw-diagram` skill under `.claude/`, whose palette is restricted to Excalidraw's stock colors so drawings adapt to the site's dark mode.

## 2026-09-01
* **Update**: Brought `references/` documents into OKF §11 conformance — both source files had HTML comment headers instead of YAML frontmatter, which §11.1 requires of every non-reserved `.md` file. They now carry `type: Reference` plus `resource`, `author`, `fidelity`, and `retrieved`.
* **Creation**: Added [Operating This Wiki](/operating-this-wiki.md), the operator's manual for this bundle — layer ownership, the prompts that drive each operation, the frontmatter fields in use, and the path from a push to the published site.
* **Update**: `log.md` is now published to the site alongside the concept pages, and carries frontmatter so it renders with a real title. OKF §8 forbids frontmatter on `index.md`; §9 sets no such rule for `log.md`.
* **Ingest**: Added [Karpathy's "LLM Wiki" gist](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/karpathy-llm-wiki.md) and the [Google Cloud OKF announcement](https://github.com/ismailkhan313/tech-llm-wiki/blob/main/references/okf-announcement-google-cloud.md) as sources. Created [The LLM Wiki Pattern](/llm-wiki-pattern.md), [Wiki Operations](/wiki-operations.md), and [Open Knowledge Format (OKF)](/open-knowledge-format.md); catalogued both sources in `references/index.md` and populated the root `index.md`. Noted that the OKF blog documents v0.1 while this bundle targets v0.2 — `timestamp` and body `# Citations` are superseded by `generated` and frontmatter `sources`.
* **Initialization**: Created the OKF-structured wiki scaffold — `index.md`, `log.md`, `references/`, and `CLAUDE.md`.
